# Core FastAPI and web framework
fastapi>=0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.18

# Database and ORM
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.3.6

# Background tasks and caching
redis==5.2.0
celery==5.4.0

# AWS and cloud storage
boto3==1.35.80

# Authentication and security
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4

# Data validation and settings
pydantic==2.10.2
pydantic-settings==2.6.1

# AI and ML
openai>=1.55.0

# Document processing - Latest versions
PyMuPDF==1.25.1  # Latest PyMuPDF for PDF processing
pdfplumber==0.11.4  # Latest pdfplumber for advanced PDF parsing
python-docx==1.1.2  # Latest python-docx for Word documents

# Image processing and OCR - Latest versions
Pillow==11.0.0  # Latest Pillow for image processing
pytesseract==0.3.13  # Latest pytesseract for OCR
opencv-python==4.10.0.84  # Added OpenCV for advanced image processing

# Additional document processing libraries
markdown==3.7  # For markdown document processing
beautifulsoup4==4.12.3  # For HTML content extraction
lxml==5.3.0  # XML/HTML parsing for document extraction

# HTTP and networking
requests==2.32.3
httpx==0.28.1  # Modern HTTP client for async operations

# Security updates - Pinned by Snyk
h11==0.16.0 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.6.0 # not directly required, pinned by Snyk to avoid a vulnerability
anyio>=4.4.0 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
certifi>=2024.8.30 # Updated certificate bundle

# Development and utilities
python-magic==0.4.27  # For better file type detection
chardet==5.2.0  # Character encoding detection
python-dateutil==2.9.0.post0  # Date parsing utilities